'''
The output should be:
80
'''
foo = 80
if foo < 30:
	print(foo)
elif foo < 100:
	print(foo)
else:
	print('big number wow')

# The if else statements have a order. 
# If is always the start or also the end when it is 1 condition statement. 
# elif (else if) is only at the middle / between if and else but can never be start or end.
# else is always and only at the end to close your condition statement.